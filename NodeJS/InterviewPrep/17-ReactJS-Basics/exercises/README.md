# React Exercises
1. Build a paginated user list with loading, empty, error, retry, and request cancellation states.
2. Build a form that displays server field-validation errors and prevents duplicate submission.
3. Implement a protected route, then explain why it cannot secure the API.
4. Profile a slow list and add virtualization rather than speculative memoization.

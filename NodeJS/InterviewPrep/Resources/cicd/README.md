# CI/CD

A safe pipeline: lint → unit tests → build → dependency/security checks → integration tests → publish artifact → deploy → smoke test → monitor/rollback. Cache dependencies, keep secrets in a secret manager, and make migrations backward compatible.

**Interview angles:** blue/green vs canary, artifacts, approvals, rollback, database migrations, and supply-chain security.

# Swagger / OpenAPI

OpenAPI describes paths, request/response schemas, auth schemes, and error contracts. Generate interactive docs, clients, and contract tests from it, but treat the source spec and implementation as a versioned compatibility contract.

**Interview angles:** backward-compatible changes, schema validation, versioning, examples, and deprecation.

# Winston and Pino

Use structured JSON logs with timestamp, level, request/trace ID, operation, and safe error metadata. Pino is low-overhead; Winston is flexible and transport-oriented. Never log secrets, credentials, raw tokens, or unnecessary PII.

**Interview angles:** log levels, redaction, retention, sampling, correlation, and logs vs metrics vs traces.

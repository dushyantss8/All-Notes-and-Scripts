# Redis

Use Redis for cache-aside reads, short-lived sessions, rate limiting, and queues—not as an unbounded primary database. Choose key names with tenant and version namespaces, set TTLs, defend against cache stampedes, and define invalidation ownership.

**Interview angles:** persistence, eviction, replication, distributed locks (carefully), pub/sub limits, and hot keys.

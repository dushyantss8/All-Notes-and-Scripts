# Debugging

Start with symptom, scope, and recent change. Reproduce safely, inspect structured logs/metrics/traces, form one falsifiable hypothesis, make the smallest corrective change, then validate and document the root cause.

**Interview angles:** event-loop lag, memory leaks, slow queries, dependency timeouts, feature flags, rollback, and blameless postmortems.

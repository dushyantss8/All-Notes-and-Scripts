# GraphQL and gRPC

GraphQL lets clients request a typed graph; prevent N+1 with batching, bound query depth/cost, and authorize resolvers. gRPC uses Protocol Buffers and supports streaming; handle deadlines, status codes, schema evolution, and gateway concerns.

**Interview angles:** GraphQL vs REST, gRPC vs HTTP/JSON, caching, observability, and version compatibility.

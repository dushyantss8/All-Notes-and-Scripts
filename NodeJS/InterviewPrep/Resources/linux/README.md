# Linux

```bash
ps aux | rg node
ss -ltnp
top
df -h && free -h
journalctl -u app -f
```

**Interview angles:** process lifecycle, permissions, signals, ports, logs, disk/memory pressure, and least privilege.

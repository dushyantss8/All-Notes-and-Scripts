# Git/GitHub

```bash
git status
git switch -c feature/api
git add -p && git commit -m "add: API validation"
git fetch origin && git rebase origin/main
```

**Interview angles:** rebase vs merge, resolving conflicts by intent, small reviewable commits, protected branches, CI checks, and avoiding secrets in history.

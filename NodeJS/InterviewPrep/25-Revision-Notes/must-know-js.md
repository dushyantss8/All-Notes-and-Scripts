# Must-Know JavaScript

Closures; lexical scope; `this`; coercion; prototypes; promises/microtasks; async/await error paths; ES modules; Map/Set; immutability; debounce/throttle; memory and cancellation.

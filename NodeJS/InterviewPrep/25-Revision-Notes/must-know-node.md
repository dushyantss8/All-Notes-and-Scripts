# Must-Know Node.js

Event loop and libuv; streams/backpressure; errors; HTTP timeouts; graceful shutdown; worker threads; config; logging/tracing; API security; queues; retries/idempotency; testing.

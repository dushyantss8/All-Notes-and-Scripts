# Express Cheat Sheet

**Order:** request ID → logging → security → parsing → auth → routes → 404 → error handler.

Validate body/query/params. Async handlers forward errors. Use consistent error envelopes. Configure proxy trust for the real topology.

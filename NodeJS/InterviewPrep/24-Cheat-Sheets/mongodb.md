# Mongodb Cheat Sheet

Use projections and cursor pagination. Index equality keys before range/sort keys. Use `explain("executionStats")`; watch for `COLLSCAN` and blocking sorts. Embed bounded co-read data; reference high-cardinality data.

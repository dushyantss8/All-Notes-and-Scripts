# Git Cheat Sheet

```bash
git status
git switch -c feature/name
git add -p
git commit -m "add: purpose"
git fetch origin
git rebase origin/main
```
Avoid rewriting shared history. Resolve conflicts by intent, test, and inspect the final diff.

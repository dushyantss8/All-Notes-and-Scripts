# Sql Cheat Sheet

`WHERE` filters rows; `HAVING` filters groups. Bind parameters. Use `EXPLAIN ANALYZE`. Composite-index order normally follows equality filters, then range/sort; verify against the plan. Prefer keyset pagination for deep pages.

# TypeScript Exercises
1. Model an API result as a discriminated union and make invalid states unrepresentable.
2. Write a generic `paginate<T>` result type.
3. Convert an `any` JSON payload to `unknown` and write a safe guard.
4. Model PATCH input with `Partial<Pick<User, "name" | "timezone">>` while rejecting unknown fields at runtime.
